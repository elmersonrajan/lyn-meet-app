<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Signalling and media. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Speaking in class. The app joins muted and asks for this before the
         first unmute; a student who refuses still hears the lesson. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>

    <!-- Routing the lesson to a headset. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>

    <!-- Keeping the screen on through an hour-long class. -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- A camera is never opened: students do not publish video, and the
         server does not relay student video to anyone. Both are marked not
         required so the Play listing does not claim hardware the app has no
         use for, and so it installs on a device without a microphone. -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>

    <application
        android:label="LYN MEET"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- The link a teacher shares with the class:
                 https://meet.lynindia.in/?lynmeet=DEVTEST

                 autoVerify is what makes this open the app directly instead of
                 showing a chooser, or worse, opening the browser. Android
                 fetches https://meet.lynindia.in/.well-known/assetlinks.json on
                 install and checks that it names this package and the
                 certificate this build is signed with. If that file is missing,
                 served as anything other than application/json, or lists a
                 fingerprint that does not match the signing key, verification
                 fails silently and every link goes to the browser instead.
                 See deploy/assetlinks.json and deploy/README.md. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="meet.lynindia.in"/>
                <!-- http as well, so a link pasted without the scheme, or an
                     older link, still lands in the app. -->
                <data android:scheme="http" android:host="meet.lynindia.in"/>
            </intent-filter>

            <!-- A custom scheme as a fallback. Verification can fail for
                 reasons outside the app - a device with no Play Services, a
                 sideloaded build signed with a different key, a captive
                 network blocking the well-known fetch - and this always opens
                 the app. readMeetingIdFromUri accepts lynmeet://join/NEET26
                 and lynmeet://?lynmeet=NEET26 alike. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="lynmeet"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
