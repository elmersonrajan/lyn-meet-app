<?xml version="1.0" encoding="utf-8"?>
<!--
  The classroom server is reached over plain http.

  Not laziness: the server presents a self-signed certificate, which a native
  Dart client rejects outright rather than prompting the way a browser does.
  The choice was between shipping a certificate override that disables
  validation for every host, or allowing cleartext to exactly one known
  address. This is the narrower hole, and it is visible in the manifest rather
  than buried in Dart.

  Everything else on the internet stays TLS-only: cleartextTrafficPermitted is
  false in base-config, so this permits one server and nothing more.

  When the backend gets a real domain and certificate, delete the
  domain-config block below and the android:networkSecurityConfig attribute in
  AndroidManifest.xml.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- The school server. -->
        <domain includeSubdomains="false">59.96.57.40</domain>
        <!-- The LAN address, for testing inside the building. -->
        <domain includeSubdomains="false">192.168.1.55</domain>
        <!-- An emulator talking to a laptop running the backend. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
